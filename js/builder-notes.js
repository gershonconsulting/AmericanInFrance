/* ============================================================
   PAGE BUILDER — generates all 40 landing pages
   Each page uses the same shell but different:
   - pillarClass, pillarLabel, emoji, title, slug
   - hero color gradient
   - keyFacts, intro, sections, resources, feeds, relatedTopics
   ============================================================ */
